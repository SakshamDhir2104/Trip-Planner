PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/tripplanner
JWT_SECRET=change-me-super-secret
JWT_EXPIRES_IN=7d

# Optional: if true, the server will attempt to seed demo data on startup
# (also useful for first run). For repeat runs, delete DB or use --seed.
SEED_ON_START=false
